# Secret Santa Environment Configuration

# Flask Secret Key (change in production)
SECRET_KEY=your-secret-key-here

# SMTP Email Configuration
# You can configure SMTP settings here OR in the admin panel
# Environment variables take precedence if database settings are empty

# Gmail Example (use App Password, not regular password):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_USE_TLS=true

# Gmail with SSL (port 465):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_USE_TLS=false

# Office 365 Example:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=your-email@company.com
# SMTP_PASSWORD=your-password
# SMTP_USE_TLS=true

# Other SMTP providers:
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_USER=your-email@domain.com
# SMTP_PASSWORD=your-password
# SMTP_USE_TLS=true

# Notes:
# - For Gmail, you MUST use an App Password, not your regular password
# - Generate App Password: Google Account -> Security -> 2-Step Verification -> App passwords
# - Port 587 uses STARTTLS (SMTP_USE_TLS=true)
# - Port 465 uses SSL from start (SMTP_USE_TLS=false)
# - Port 25 is usually blocked by ISPs
